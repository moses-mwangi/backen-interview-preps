// // tests/setup.ts
// import "dotenv/config"; // To load environment variables from .env if necessary
// import { server } from "./testServer"; // Optional setup to start a mock server for testing

// beforeAll(async () => {
//   // Run any setup tasks before tests
//   console.log("Setting up tests...");
// });

// afterAll(async () => {
//   // Run cleanup tasks after tests
//   console.log("Cleaning up after tests...");
// });
